{
  "name": "dgp1130-blog",
  "version": "0.0.0",
  "description": "Personal blog for https://github.com/dgp1130.",
  "main": "index.js",
  "scripts": {
    "start": "npm run -s clean && npm run -s build:ts && eleventy --serve",
    "build": "npm run -s clean && npm run -s build:ts && eleventy",
    "build:ts": "tsc -p tsconfig.app.json",
    "build:test": "tsc -p tsconfig.test.json",
    "test": "npm run -s clean && npm run -s build:test && jasmine --config=jasmine.config.json",
    "clean": "rm -rf _site/ && find src/ \\( -name '*.js' -o -name '*.js.map' \\) -delete",
    "netlify": "netlify"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/dgp1130/blog.git"
  },
  "keywords": [
    "blog",
    "11ty"
  ],
  "author": "Doug Parker",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/dgp1130/blog/issues"
  },
  "homepage": "https://github.com/dgp1130/blog#readme",
  "devDependencies": {
    "@11ty/eleventy": "^0.11.0",
    "@types/jasmine": "^3.5.11",
    "@types/node": "^14.0.26",
    "@types/nodegit": "^0.26.7",
    "jasmine": "^3.5.0",
    "netlify-cli": "^2.58.0",
    "nodegit": "^0.26.5",
    "typescript": "^3.9.7"
  }
}
