{
    "extends": "./tsconfig.base.json",
    "include": [ "src/**/*.ts" ],
    "exclude": [ "src/**/*_test.ts" ],
    "compilerOptions": {
        // Only allow Node types, no testing types like Jasmine.
        "types": [ "node" ]
    }
}
